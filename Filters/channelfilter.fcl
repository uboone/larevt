#
# Standard channel filter service configuration
#
#
BEGIN_PROLOG

standard_channel_filter: {
  service_provider: SimpleChannelStatusService
  
  # list here the bad channels:
  BadChannels:   []
  
  # list here the noisy channels:
  NoisyChannels: []
  
} # standard_channel_filter

END_PROLOG
